# Sphere-only app icon variants

Pure circle on transparent canvas (1024×1024). No squircle tile — corners are transparent so the orb floats in the macOS dock mask.

Apply one:
```bash
./scripts/generate-icons.sh build/logo-variants/sphere-only/<name>.png
```
