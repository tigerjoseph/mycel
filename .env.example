# Mycel release — copy to .env (gitignored). GitHub auth uses `gh auth token`.
APPLE_ID=
APPLE_APP_SPECIFIC_PASSWORD=
APPLE_TEAM_ID=8WH2Q45J83

# Google Calendar OAuth — Desktop app client secret from Google Cloud Console.
# Redirect URIs must include http://localhost:3000/oauth2callback through :3009/oauth2callback
GOOGLE_OAUTH_CLIENT_SECRET=
